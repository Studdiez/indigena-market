'use client';

import { Eye } from 'lucide-react';

type Props = {
  title: string;
  price: string;
  description: string;
  image: string;
  cta: string;
  onView: () => void;
};

export default function LanguageHeritagePremiumPlacementCard({
  title,
  price,
  description,
  image,
  cta,
  onView
}: Props) {
  return null;
}
