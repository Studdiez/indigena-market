import { redirect } from 'next/navigation';

export const metadata = {
  title: 'Launchpad | Indigena Global Market'
};

export default function DigitalChampionsLegacyPage() {
  redirect('/launchpad');
}
